# Notebook guidance

Run the main pipeline first:

```bash
python scripts/run_synthetic_rf_owc_lab.py
```

Then open the generated CSV files in `outputs/results/` for exploratory analysis.
The repository keeps notebook guidance lightweight so CI remains fast and deterministic.
